IR Assignment-1
-SUBHANI SHAIK(MT18117)

1. Inverted Index
	- load the saved files(inverted index dictionary file and document ids file which have .sav extension) using joblib.
	- run necessary cells in code file
	- execute search query block then it will prompt for user's choice for type of boolean query. User have to enter his/her choice and need to enter the Query terms x and y(which are single word each).
		- it will retreive documents according to boolean query.

2. Positional index and Phrase query:
	- load the saved files(positional index dictionary file and document ids file which have .sav extension) using joblib.
	- execute necessary cells in code
	- execute search query and enter Phrase query- then it will retreive documents which have this Phrase query otherwise retreives none.