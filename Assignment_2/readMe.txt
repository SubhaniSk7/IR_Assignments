									IR Assignment-2


- done the pre-processing using NLTK library.
- the numerical and text format are considered seperately.(100 != hundred). (didn't converted num2words)-not handled.


Execution:
1. retreiving top K documents based on Tf-Idf:
	- load the required .sav files.
	- run necessary cells in python notebook to give input.
	- will retrieve top K documents w.r.t query.
	

2. retreiving top K documents based on Cosine similarities based on Tf-Idf values:
	- load the required .sav files which are saved when 1st question is executed.
	- run necessary cells in python notebook to give input.
	- will retrieve top K documents w.r.t query.
	